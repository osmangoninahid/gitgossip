name: 🚀 Release GitGossip

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: 🧾 Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: ⚙️ Install uv
        run: pip install uv

      - name: 🏗️ Build and publish to PyPI
        env:
          UV_PUBLISH_TOKEN: ${{ secrets.PYPI_TOKEN }}
        run: |
          uv build
          uv publish --token $UV_PUBLISH_TOKEN

      - name: 🏷️ Create GitHub Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: 🍺 Bump Homebrew Formula
        if: startsWith(github.ref, 'refs/tags/')
        uses: dawidd6/action-homebrew-bump-formula@v3
        env:
          # Third-party taps are untrusted by default in newer Homebrew; allow CI to load the formula.
          HOMEBREW_NO_REQUIRE_TAP_TRUST: "1"
        with:
          token: ${{ secrets.HOMEBREW_TAP_GITHUB_TOKEN }}
          formula: gitgossip
          tap: osmangoninahid/homebrew-gitgossip
          tag: ${{ github.ref_name }}
          message: "chore: bump formula for ${{ github.ref_name }}"

